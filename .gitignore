# ── Runtime data (personal / generated) ────────────────────────────────────
nora_memory.json
nora_knowledge.json
nora_user_model.json
nora_cognitive_db/

# ── Logs ────────────────────────────────────────────────────────────────────
*.log
hook_notify.log

# ── Environment & secrets ────────────────────────────────────────────────────
.env
*.env
.env.*

# ── Personal / local-only files ─────────────────────────────────────────────
Personal_Jarvis_System_Design.md
graphify-out/
temp_final/
test_mic.py
.graphify_analysis.json
.graphify_python

# Claude Code session data
.claude/

# Root-level loose .txt files (recovery codes, scratch notes, etc.)
# Negation is required: the bare /*.txt rule silently swallowed requirements.txt,
# which is why the dependency manifest looked absent from outside the repo.
/*.txt
!requirements.txt

# ── Sounds (personal music library) ─────────────────────────────────────────
sounds/*.mp3
sounds/*.wav
sounds/*.ogg
sounds/*.flac
sounds/*.m4a

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd

# ── Build / packaging ───────────────────────────────────────────────────────
dist/
build/
*.egg-info/
*.egg
.eggs/

# ── Testing ──────────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
.cursor/
*.sublime-project
*.sublime-workspace

# ── OS ───────────────────────────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
ydotool/
nora_session_state.json
nora_tasks.json
nora_audit_log.jsonl
nora_consolidation_state.json
nora_daily_brief.json
.agents/
nora_tool_trust.json

# Off-turn work and recall state (jobs / scheduler / FTS5 session index)
nora_jobs.json
nora_schedules.json
nora_sessions.db

nora_consent_log.json
nora_reversible_log.json
nora_user_preferences.json
nora_model_router_state.json
nora_model_router_log.jsonl
TELEMACHUS_PORT_PLAN.md

# Google OAuth — client secret and the token minted from it. Neither belongs in
# a repo: the token grants calendar + gmail.modify until it is revoked.
credentials.json
google_token.json
